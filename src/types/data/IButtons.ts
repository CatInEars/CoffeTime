export interface IButtons {
  image: any,
  title: string,
  backgroundColor: string,
  textColor: string,
  onPressBackgroundColor: string,
  activateLogin: boolean,
  imageWidth?: number,
  imageHeight?: number
}